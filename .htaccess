<IfModule mod_rewrite.c>
  RewriteEngine On

  # si le fichier ou dossier existe, on laisse Apache le servir
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # sinon, tout passe par Kirby
  RewriteRule ^ index.php [L]
</IfModule>
